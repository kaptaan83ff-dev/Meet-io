services:
  # ------------------------------------
  # 1. Backend Service (Node.js)
  # ------------------------------------
  - type: web
    name: meet-io-server
    env: node
    rootDir: server
    buildCommand: npm install && npm run build
    startCommand: npm start
    plan: free # Optional: specify plan type
    envVars:
      - key: NODE_ENV
        value: production
      # You will need to add these manually in the Render Dashboard:
      # - CLIENT_URL (The URL of your static site, e.g. https://meet-io-client.onrender.com)
      # - MONGODB_URI
      # - LIVEKIT_API_KEY
      # - LIVEKIT_API_SECRET
      # - LIVEKIT_URL
      # - REDIS_URL (Optional)
      # - LIVEKIT_API_KEY
      # - LIVEKIT_API_SECRET
      # - LIVEKIT_URL
      # - REDIS_URL (Optional)

  # ------------------------------------
  # 2. Frontend Service (React/Vite)
  # ------------------------------------
  - type: static
    name: meet-io-client
    rootDir: client
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_URL
        fromService:
          type: web
          name: meet-io-server
          property: url
    routes:
      - type: rewrite
        source: /**
        destination: /index.html
